---
const stats = [
  { nombre: "70k+", label: "Élèves satisfaits" },
  { nombre: "175k+", label: "Abonnés" },
  { nombre: "1000+", label: "Leçons vidéo" },
  { nombre: "4.9/5", label: "Satisfaction" }
];

const features = [
  { icon: "🎸", text: "Cours adaptés à votre niveau" },
  { icon: "🎥", text: "Vidéos HD accessibles à vie" },
  { icon: "🎹", text: "Support personnalisé" },
  { icon: "🎼", text: "Certification incluse" }
];
---

<section class="hero">
  <div class="hero-background">
    <div class="gradient-overlay"></div>
    <div class="pattern-overlay"></div>
  </div>
  
  <div class="container">
    <div class="hero-content">
      <div class="hero-text-content">
        <h1 class="hero-title">
          <span class="text-gradient">Apprenez la musique</span>
          <br />
          à votre rythme
        </h1>
        <p class="hero-subtitle">Découvrez une nouvelle façon d'apprendre la musique avec nos cours en vidéo, suivis par plus de 70 000 élèves passionnés.</p>
      
        <div class="cta-group">
          <a href="/cours" class="btn btn-primary pulsing">
            Commencer maintenant
            <span class="price-tag">À partir de 19.99€/mois</span>
          </a>
          <a href="/essai-gratuit" class="btn btn-outline">
            Essai gratuit 7 jours
            <span class="arrow">→</span>
          </a>
        </div>

        <div class="features-list">
          {features.map(feature => (
            <div class="feature-item floating">
              <span class="feature-icon">{feature.icon}</span>
              <span class="feature-text">{feature.text}</span>
            </div>
          ))}
        </div>
      </div>
    </div>

    <div class="stats-container">
      {stats.map(stat => (
        <div class="stat-item">
          <span class="stat-number text-gradient">{stat.nombre}</span>
          <span class="stat-label">{stat.label}</span>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    overflow: hidden;
    padding: 8rem 0 4rem;
  }

  .hero-background {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image: url('https://test.musique-facile.fr/images/hero_banner_apprentissage_musique.jpg');
    background-size: cover;
    background-position: center;
    z-index: -2;
  }

  .gradient-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(
      135deg,
      rgba(0, 0, 0, 0.85) 0%,
      rgba(0, 0, 0, 0.75) 100%
    );
    z-index: -1;
  }

  .pattern-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.05'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
    opacity: 0.1;
    z-index: -1;
  }

  .hero-content {
    position: relative;
    z-index: 1;
    animation: fadeIn 1s ease-out;
  }

  .hero-text-content {
    max-width: 700px;
  }

  .hero-title {
    font-size: clamp(2.5rem, 5vw, 4.5rem);
    line-height: 1.1;
    margin-bottom: 1.5rem;
    color: var(--color-white);
    text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
  }

  .hero-subtitle {
    font-size: clamp(1rem, 2vw, 1.35rem);
    color: rgba(255, 255, 255, 0.9);
    margin-bottom: 2.5rem;
    line-height: 1.6;
    text-shadow: 0 1px 2px rgba(0, 0, 0, 0.2);
  }

  .cta-group {
    display: flex;
    gap: 1.5rem;
    margin-bottom: 3rem;
    flex-wrap: wrap;
  }

  .btn-primary {
    padding: clamp(0.8rem, 2vw, 1.2rem) clamp(1.5rem, 3vw, 2.5rem);
    font-size: clamp(1rem, 1.5vw, 1.2rem);
    box-shadow: 0 4px 15px rgba(0, 225, 167, 0.3);
  }

  .price-tag {
    display: block;
    font-size: clamp(0.8rem, 1.2vw, 0.9rem);
    opacity: 0.95;
    margin-top: 0.3rem;
  }

  .features-list {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 1.5rem;
  }

  .feature-item {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1.2rem;
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: var(--border-radius);
    backdrop-filter: blur(10px);
    transition: all var(--transition-base);
  }

  .feature-icon {
    font-size: 1.8rem;
    filter: drop-shadow(0 2px 4px rgba(0, 0, 0, 0.2));
  }

  .feature-text {
    color: var(--color-white);
    font-size: clamp(0.9rem, 1.5vw, 1.1rem);
    font-weight: 500;
    text-shadow: 0 1px 2px rgba(0, 0, 0, 0.2);
  }

  .stats-container {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: clamp(1rem, 2vw, 2rem);
    margin-top: 4rem;
    background: rgba(255, 255, 255, 0.1);
    padding: clamp(1.5rem, 3vw, 2rem);
    border-radius: var(--border-radius);
    backdrop-filter: blur(10px);
  }

  .stat-item {
    text-align: center;
    padding: clamp(1rem, 2vw, 1.5rem);
    background: rgba(255, 255, 255, 0.05);
    border-radius: var(--border-radius);
    transition: transform var(--transition-base);
  }

  .stat-number {
    display: block;
    font-size: clamp(1.8rem, 3vw, 2.8rem);
    font-weight: bold;
    margin-bottom: 0.5rem;
    background: linear-gradient(135deg, var(--theme-main), var(--theme-piano));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    text-shadow: none;
  }

  .stat-label {
    display: block;
    color: var(--color-white);
    font-size: clamp(0.9rem, 1.5vw, 1.1rem);
    font-weight: 500;
    opacity: 0.95;
  }

  @media (max-width: 768px) {
    .hero {
      padding: 6rem 0 3rem;
      min-height: auto;
    }

    .cta-group {
      flex-direction: column;
    }

    .btn {
      width: 100%;
      text-align: center;
    }

    .features-list {
      grid-template-columns: 1fr;
    }

    .stats-container {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 480px) {
    .stats-container {
      grid-template-columns: 1fr;
    }

    .stat-item {
      padding: 1rem;
    }
  }
</style>