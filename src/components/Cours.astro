---
const cours = [
  {
    titre: "Cours de Guitare",
    description: "Apprenez à jouer de la guitare avec des cours adaptés à votre niveau",
    image: "/images/cours/cours-de-guitare.jpg",
    couleur: "var(--color-primary)",
    icon: "🎸",
    features: ["Accords de base", "Techniques avancées", "Notes et gammes"],
    url: "/cours/cours-de-guitare"
  },
  {
    titre: "Cours de Piano",
    description: "Découvrez le piano à travers des leçons personnalisées",
    image: "/images/cours/cours-de-piano.jpg",
    couleur: "var(--color-piano)",
    icon: "🎹",
    features: ["Lecture de partitions", "Technique des doigts", "Harmonie"],
    url: "/cours/cours-de-piano"
  },
  {
    titre: "Cours de Ukulélé",
    description: "Initiez-vous au ukulélé dans une ambiance décontractée",
    image: "/images/cours/cours-de-ukulele.jpg",
    couleur: "var(--color-ukulele)",
    icon: "🏖️",
    features: ["Accords simples", "Rythmiques", "Shuffle"],
    url: "/cours/cours-de-ukulele"
  },
  {
    titre: "Cours de Solfège",
    description: "Maîtrisez les bases de la théorie musicale",
    image: "/images/cours/cours-de-solfege.jpg",
    couleur: "var(--color-solfege)",
    icon: "🎼",
    features: ["Lecture de notes", "Rythme", "Cycle des Quintes "],
    url: "/cours/cours-de-solfege"
  }
];
---

<section class="cours section-padding">
  <div class="container">
    <div class="section-header">
      <h2 class="section-title">Nos Cours de Musique</h2>
      <p class="section-subtitle">Choisissez votre instrument et commencez votre voyage musical</p>
    </div>

    <div class="cours-grid">
      {cours.map(cours => (
        <a href={cours.url} class="cours-card" style={`--cours-color: ${cours.couleur}`}>
          <div class="cours-image-wrapper">
            <img src={cours.image} alt={cours.titre} />
            <div class="cours-overlay">
              <span class="cours-icon">{cours.icon}</span>
            </div>
          </div>
          
          <div class="cours-content">
            <h3>{cours.titre}</h3>
            <p class="cours-description">{cours.description}</p>
            
            <ul class="cours-features">
              {cours.features.map(feature => (
                <li>
                  <span class="feature-dot"></span>
                  {feature}
                </li>
              ))}
            </ul>

            <div class="btn-cours">
              Découvrir nos cours
              <span class="arrow">→</span>
            </div>
          </div>
        </a>
      ))}
    </div>
  </div>
</section>

<style>
  .cours {
    background: var(--bg-light);
    padding: 8rem 0;
  }

  .cours-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2rem;
    margin-top: 4rem;
  }

  .cours-card {
    background: var(--color-white);
    border-radius: var(--border-radius-lg);
    overflow: hidden;
    box-shadow: var(--box-shadow);
    transition: all var(--transition-base);
    position: relative;
    text-decoration: none;
    color: inherit;
  }

  .cours-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: var(--cours-color);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform var(--transition-base);
  }

  .cours-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
  }

  .cours-card:hover::before {
    transform: scaleX(1);
  }

  .cours-image-wrapper {
    position: relative;
    height: 200px;
    overflow: hidden;
  }

  .cours-image-wrapper img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform var(--transition-base);
  }

  .cours-card:hover .cours-image-wrapper img {
    transform: scale(1.1);
  }

  .cours-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(to top, rgba(0, 0, 0, 0.7), transparent);
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    transition: opacity var(--transition-base);
  }

  .cours-card:hover .cours-overlay {
    opacity: 1;
  }

  .cours-icon {
    font-size: 3rem;
    transform: translateY(20px);
    transition: transform var(--transition-base);
  }

  .cours-card:hover .cours-icon {
    transform: translateY(0);
  }

  .cours-content {
    padding: 2rem;
  }

  .cours-content h3 {
    color: var(--cours-color);
    font-size: 1.5rem;
    margin-bottom: 1rem;
  }

  .cours-description {
    color: var(--color-dark);
    opacity: 0.8;
    margin-bottom: 1.5rem;
    line-height: 1.6;
  }

  .cours-features {
    list-style: none;
    padding: 0;
    margin: 0 0 2rem;
  }

  .cours-features li {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.5rem;
    color: var(--color-dark);
    opacity: 0.8;
  }

  .feature-dot {
    width: 8px;
    height: 8px;
    background: var(--cours-color);
    border-radius: 50%;
  }

  .btn-cours {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--cours-color);
    font-weight: 600;
    transition: all var(--transition-base);
  }

  .arrow {
    transition: transform var(--transition-base);
  }

  .cours-card:hover .arrow {
    transform: translateX(5px);
  }

  @media (max-width: 1200px) {
    .cours-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .cours-grid {
      grid-template-columns: 1fr;
    }

    .cours {
      padding: 4rem 0;
    }
  }
</style>