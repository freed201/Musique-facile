---
import OptimizedImage from './OptimizedImage.astro';
---

<section class="hero">
  <div class="hero-background">
    <div class="gradient-overlay"></div>
    <div class="pattern-overlay"></div>
  </div>
  
  <div class="container">
    <div class="hero-content">
      <div class="hero-main">
        <div class="hero-text-content">
          <h1 class="hero-title">
            <span class="text-gradient">Jouez Votre Premier Morceau en 7 Jours</span>
          </h1>
          <p class="hero-subtitle">Plus de 80 000 élèves ont transformé leur rêve musical en réalité</p>

          <!-- Trust Badges -->
          <div class="trust-badges">
            <div class="trust-badge">
              <span class="trust-icon">⭐</span>
              <span class="trust-text"><strong>4.7/5</strong> (2,847 avis)</span>
            </div>
            <div class="trust-badge">
              <span class="trust-icon">🏆</span>
              <span class="trust-text">Élu <strong>meilleur site 2024</strong></span>
            </div>
            <div class="trust-badge">
              <span class="trust-icon">🔒</span>
              <span class="trust-text"><strong>Paiement sécurisé</strong></span>
            </div>
          </div>

          <div class="cta-group">
            <a href="/cours" class="cta-button cta-primary">
              <span class="cta-text">Commencer Gratuitement</span>
              <span class="cta-arrow">→</span>
              <div class="cta-shine"></div>
            </a>
            <button class="cta-button cta-secondary" id="demo-button">
              <span class="cta-icon">▶</span>
              <span class="cta-text">Voir comment ça marche</span>
              <span class="cta-duration">(2 min)</span>
            </button>
          </div>

          <!-- Micro-copy under CTAs -->
          <div class="cta-benefits">
            <span>✓ Aucune carte bancaire</span>
            <span>✓ Accès immédiat</span>
            <span>✓ Garantie 15 jours</span>
          </div>
        </div>

        <div class="hero-image">
          <OptimizedImage
            src="/images/cours-piano-guitare-ukulele.webp"
            alt="Cours de piano, guitare et ukulélé - Plus de 80 000 élèves formés"
            width={600}
            height={400}
            loading="eager"
            quality={90}
          />
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    overflow: hidden;
    padding: 4rem 0 2rem; /* Réduction des marges verticales */
  }

  .hero-background {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image: url('/images/hero.jpg');
    background-size: cover;
    background-position: center;
    z-index: -2;
  }

  .gradient-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(
      135deg,
      rgba(0, 0, 0, 0.85) 0%,
      rgba(0, 0, 0, 0.75) 100%
    );
    z-index: -1;
  }

  .pattern-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.05'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
    opacity: 0.1;
    z-index: -1;
  }

  .hero-content {
    position: relative;
    z-index: 1;
  }

  .hero-main {
    display: grid;
    grid-template-columns: 1.2fr 0.8fr;
    gap: 4rem;
    align-items: center;
  }

  .hero-text-content {
    max-width: 700px;
  }

  .hero-title {
    font-size: 4.5rem;
    line-height: 1.1;
    margin-bottom: 1.5rem;
    color: var(--color-white);
    text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
  }

  .hero-subtitle {
    font-size: 1.5rem;
    color: var(--color-white);
    opacity: 0.95;
    margin-bottom: 2rem;
    line-height: 1.4;
    text-shadow: 0 1px 2px rgba(0, 0, 0, 0.2);
  }

  /* Trust Badges */
  .trust-badges {
    display: flex;
    gap: 2rem;
    margin-bottom: 2.5rem;
    flex-wrap: wrap;
  }

  .trust-badge {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--color-white);
    font-size: 0.95rem;
  }

  .trust-icon {
    font-size: 1.3rem;
  }

  .trust-text strong {
    color: var(--theme-current);
  }

  /* CTA Group */
  .cta-group {
    display: flex;
    gap: 1.5rem;
    margin-bottom: 1.5rem;
    flex-wrap: wrap;
  }

  .cta-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.8rem;
    position: relative;
    padding: 1.5rem 2.5rem;
    border-radius: var(--border-radius-lg);
    text-decoration: none;
    overflow: hidden;
    transition: all 0.3s ease;
    font-weight: 700;
    font-size: 1.2rem;
    border: none;
    cursor: pointer;
    font-family: 'Poppins', sans-serif;
  }

  .cta-primary {
    background: var(--theme-current);
    color: white;
    box-shadow: 0 8px 20px rgba(0, 194, 142, 0.4);
    min-width: 280px;
  }

  .cta-primary:hover {
    transform: translateY(-5px);
    box-shadow: 0 15px 30px rgba(0, 194, 142, 0.5);
    background: var(--theme-current-dark);
  }

  .cta-secondary {
    background: transparent;
    color: white;
    border: 2px solid rgba(255, 255, 255, 0.5);
    backdrop-filter: blur(10px);
  }

  .cta-secondary:hover {
    border-color: white;
    background: rgba(255, 255, 255, 0.1);
    transform: translateY(-3px);
  }

  .cta-arrow {
    font-size: 1.4rem;
    transition: transform 0.3s ease;
  }

  .cta-button:hover .cta-arrow {
    transform: translateX(5px);
  }

  .cta-icon {
    font-size: 0.9rem;
  }

  .cta-duration {
    font-size: 0.85rem;
    opacity: 0.8;
    font-weight: 400;
  }

  .cta-text {
    position: relative;
    z-index: 1;
  }

  /* CTA Benefits */
  .cta-benefits {
    display: flex;
    gap: 2rem;
    color: var(--color-white);
    font-size: 0.95rem;
    opacity: 0.9;
    flex-wrap: wrap;
  }

  .cta-benefits span {
    display: flex;
    align-items: center;
  }

  .cta-shine {
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      120deg,
      transparent,
      rgba(255, 255, 255, 0.3),
      transparent
    );
    animation: shine 3s infinite;
  }

  @keyframes shine {
    0% { left: -100%; }
    20% { left: 100%; }
    100% { left: 100%; }
  }

  .cta-button:hover {
    transform: translateY(-5px);
    box-shadow: 0 15px 30px rgba(0, 0, 0, 0.3);
    background: var(--theme-current-dark);
  }

  .hero-image {
    display: flex;
    justify-content: flex-end;
    align-items: flex-start;
  }

  .hero-image img {
    max-width: 100%;
    height: auto;
    border-radius: var(--border-radius);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
    transition: transform 0.3s ease;
  }
  
  .hero-image img:hover {
    transform: scale(1.02);
  }

  @media (max-width: 1024px) {
    .hero-main {
      grid-template-columns: 1fr;
    }

    .hero-image {
      display: none;
    }

    .hero-title {
      font-size: 3.5rem;
    }
  }

  @media (max-width: 768px) {
    .hero {
      padding: 3rem 0 2rem;
    }

    .hero-title {
      font-size: 2.8rem;
    }

    .hero-subtitle {
      font-size: 1.2rem;
    }

    .trust-badges {
      flex-direction: column;
      gap: 1rem;
    }

    .cta-group {
      flex-direction: column;
      width: 100%;
    }

    .cta-button {
      width: 100%;
      justify-content: center;
    }

    .cta-primary {
      min-width: unset;
    }

    .cta-benefits {
      flex-direction: column;
      gap: 0.8rem;
    }
  }

  @media (max-width: 480px) {
    .hero-title {
      font-size: 2.2rem;
    }

    .hero-subtitle {
      font-size: 1.1rem;
    }

    .cta-button {
      padding: 1.2rem 2rem;
      font-size: 1.1rem;
    }
  }
</style>
