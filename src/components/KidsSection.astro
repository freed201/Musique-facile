---
const courses = [
  {
    title: "Guitare Kids",
    description: "Un apprentissage ludique de la guitare pour les 6-12 ans",
    image: "/images/guitare-kids.webp",
    features: [
      "Méthode adaptée aux petites mains",
      "Progression par jeux et challenges",
      "Récompenses et badges à gagner"
    ],
    icon: "🎸",
    color: "var(--guitar-500)",
    url: "https://kids.musique-facile.fr/cours-guitare-debutant-enfant"
  },
  {
    title: "Piano Kids",
    description: "Découvrir le piano en s'amusant dès 6 ans",
    image: "/images/piano-kids.webp",
    features: [
      "Exercices interactifs et colorés",
      "Histoires musicales captivantes",
      "Suivi des progrès personnalisé"
    ],
    icon: "🎹",
    color: "var(--piano-500)",
    url: "https://kids.musique-facile.fr/cours-piano-debutant-enfant"
  },
  {
    title: "Ukulélé Kids",
    description: "L'instrument parfait pour débuter la musique dès 6 ans",
    image: "/images/ukulele-kids.webp",
    features: [
      "Instrument adapté aux enfants",
      "Apprentissage par le jeu",
      "Premières mélodies rapidement"
    ],
    icon: "🏖️",
    color: "var(--ukulele-500)",
    url: "https://kids.musique-facile.fr/cours-ukulele-debutant-enfant"
  }
];
---

<section class="kids-section">
  <div class="container">
    <div class="section-header">
      <span class="section-badge">Pour les 6-12 ans</span>
      <h2>La musique à la portée des enfants</h2>
    </div>

    <div class="courses-grid">
      {courses.map(course => (
        <a href={course.url} class="course-card" target="_blank" rel="noopener noreferrer"  style={`--card-color: ${course.color}`}>
          <div class="card-header">
            <span class="course-icon">{course.icon}</span>
            <h3>{course.title}</h3>
          </div>
          <p class="course-description">{course.description}</p>
          <ul class="course-features">
            {course.features.map(feature => (
              <li>{feature}</li>
            ))}
          </ul>
          <span class="discover-btn">Découvrir →</span>
        </a>
      ))}
    </div>

   <div class="kids-cta">
  <a href="https://kids.musique-facile.fr" class="btn-primary" target="_blank" rel="noopener noreferrer">
    Découvrir Musique Facile Kids
    <span class="arrow">→</span>
  </a>
</div>

      <p class="cta-note">15 jours d'essai gratuit</p>
    </div>
  </div>
</section>

<style>
  .kids-section {
    padding: var(--space-20) 0;
    background: linear-gradient(135deg, #FF6B6B 0%, #FFE66D 100%);
    position: relative;
    overflow: hidden;
  }

  .kids-section::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.1'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
    opacity: 0.15;
  }

  .container {
    position: relative;
    z-index: 1;
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 var(--space-6);
  }

  .section-header {
    text-align: center;
    margin-bottom: var(--space-12);
  }

  .section-badge {
    display: inline-block;
    background: white;
    color: #FF6B6B;
    padding: var(--space-2) var(--space-4);
    border-radius: var(--radius-full);
    font-size: var(--text-sm);
    font-weight: var(--font-semibold);
    margin-bottom: var(--space-4);
    box-shadow: var(--shadow-md);
  }

  .section-header h2 {
    color: white;
    font-size: clamp(var(--text-3xl), 4vw, var(--text-5xl));
    font-weight: var(--font-extrabold);
    margin: 0;
    line-height: var(--leading-tight);
    text-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
  }

  .courses-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-8);
    margin-bottom: var(--space-16);
  }

  .course-card {
    position: relative;
    background: white;
    border-radius: var(--radius-2xl);
    padding: var(--space-8);
    text-decoration: none;
    color: inherit;
    transition: var(--transition-base);
    display: flex;
    flex-direction: column;
    box-shadow: var(--shadow-md);
    border: 2px solid rgba(255, 255, 255, 0.8);
    overflow: hidden;
  }

  .course-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: var(--card-color);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform var(--duration-base) var(--ease-out);
  }

  .course-card:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow-2xl);
  }

  .course-card:hover::before {
    transform: scaleX(1);
  }

  .card-header {
    display: flex;
    align-items: center;
    gap: var(--space-4);
    margin-bottom: var(--space-5);
    padding: var(--space-3);
    border-radius: var(--radius-xl);
    background: linear-gradient(135deg, rgba(255,255,255,0.9) 0%, rgba(255,255,255,0.5) 100%);
    border: 1px solid rgba(255, 255, 255, 0.6);
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.05);
  }

  .course-icon {
    font-size: 2.1rem;
    background: linear-gradient(135deg, var(--card-color) 0%, rgba(255,255,255,0.2) 100%);
    width: 60px;
    height: 60px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: var(--radius-full);
    box-shadow: 0 10px 24px rgba(0, 0, 0, 0.12);
  }

  .card-header h3 {
    color: var(--card-color);
    font-size: clamp(1.3rem, 2vw, 1.6rem);
    font-weight: var(--font-extrabold);
    margin: 0;
    letter-spacing: 0.4px;
  }

  .course-description {
    font-size: var(--text-base);
    color: var(--gray-700);
    margin-bottom: var(--space-4);
    line-height: var(--leading-relaxed);
  }

  .course-features {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-6);
    flex-grow: 1;
  }

  .course-features li {
    font-size: var(--text-sm);
    color: var(--gray-600);
    margin-bottom: var(--space-2);
    padding-left: var(--space-5);
    position: relative;
  }

  .course-features li::before {
    content: '•';
    position: absolute;
    left: 0;
    color: var(--card-color);
    font-size: 1.2rem;
  }

  .discover-btn {
    color: var(--card-color);
    font-weight: var(--font-semibold);
    font-size: var(--text-sm);
    transition: transform var(--duration-base) var(--ease-out);
    display: inline-block;
  }

  .course-card:hover .discover-btn {
    transform: translateX(5px);
  }

  .kids-cta {
    text-align: center;
  }

  .btn-primary {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    background: white;
    color: #FF6B6B;
    padding: var(--space-4) var(--space-8);
    border-radius: var(--radius-lg);
    text-decoration: none;
    font-weight: var(--font-semibold);
    transition: var(--transition-base);
    box-shadow: var(--shadow-lg);
  }

  .btn-primary:hover {
    transform: translateY(-3px);
    box-shadow: 0 12px 28px rgba(0, 0, 0, 0.15);
  }

  .cta-note {
    color: white;
    font-size: var(--text-sm);
    margin-top: var(--space-3);
    opacity: 0.95;
    text-shadow: 0 1px 2px rgba(0, 0, 0, 0.1);
  }

  @media (max-width: 992px) {
    .courses-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .courses-grid {
      grid-template-columns: 1fr;
    }

    .course-card {
      padding: var(--space-6);
    }
  }
</style>
